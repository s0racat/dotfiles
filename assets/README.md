
```bash
echo 'permit persist :wheel' | doas tee /etc/doas.conf
```

```bash
paru -Sy --sudo doas --needed $(shell grep -v '^#' pkglist.txt)
```

```bash
./enable-service.sh
```

```bash
./setup-usbguard.sh
```
